/*

echo ===> for printing something in terminal

echo num=5

echo $num






echo $((num+num2))

test.sh => It's a bash file for running terminal code

pwd => current working directory
whoami => cmd for your Username
cd => change directory ../ (a level up)
cd ~/ => for home directory which is inside program files -> users

touch => for creating files touch index.html style.css index.js
mkdir => for creating directories

cp => for copy command cp filename path cp app.js src1 (path)
mv => move command mv filename path mv app.js src2 (path)
rename file => we are using mv command for it just add file name in the path

rm => for deleting file rm filename
rmdir => for removing directory rmdir foldername (if it is empty) otherwise use -r flag rmdir -r filename



/////////////////////// Viewing and Editing files in terminal (cat, nana, vim)


cat filename for viewing the file
cat => concatinate when file is big print small file in chink


nano   command for view and edti as well
by default comes with bash
ctrl + x then press y give name enter and your file will save
nklnkn
vim editor just know the name
for customisinf promt string do PS1 variable in bashrc file
*/






 
